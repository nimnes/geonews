<div class="row">
<div class="span8">
	<% if @news.any? %>
		<h3>Новости (<%= @news.count %>)</h3>
		<ol class="news">
			<%= render 'layouts/news_entry' %>
		</ol>
		<%= will_paginate @news %>
	<% end %>
</div>
</div>
